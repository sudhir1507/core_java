package interfaces;
/*
 * interface is a collection of abstraction methods
 * class can implement interface
 * interface can extend from another class
 * non abstract class extending from an abstract class needs to implement unimplemented method
 * non abstract class implementing an interface needs to implement unimplemented methods
 * interface can be instantiated
 * interface does not have a constructor
 * in addition to abstract methods interface can contain static and default methods
 * interface cannot have a data members. all variables by default considered as static and final
 * everything is public by default
 */
public class Interface_Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
